apiVersion: topic.kafka.crossplane.io/v1alpha1
kind: Topic
metadata:
  name: sample-topic
spec:
  forProvider:
    replicationFactor: 1
    partitions: 1
## Optional TopicName Config Parameter
## Overrides the default use of metadata.name with the specified string
#    topicName: "sample_topic"
## Optional Admin Config Parameters
## Full list of parameter overrides can be found here:
## https://docs.confluent.io/platform/current/installation/configuration/topic-configs.html
#    config:
#      cleanup.policy: "delete"
#      compression.type: "snappy"
  providerConfigRef:
    name: example